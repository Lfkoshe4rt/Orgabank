import { Message as MessageStyled } from "./styled";

const Message = ({ children }) => {
  return <MessageStyled>{children}</MessageStyled>;
};

//Aún no hay cajas registradas click en el boton para agregar una

export default Message;
